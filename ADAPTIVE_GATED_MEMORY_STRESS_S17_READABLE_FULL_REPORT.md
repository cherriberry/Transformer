# 自适应门控事实记忆实验报告：模型记住了什么，为什么仍会答错

更新时间：2026-09-09  
实验版本：`adaptive_fact_memory_selective_stress_v2`  
随机种子：17  
实验状态：Gated、Fixed-LRU、SWA-only 三个条件全部完成

## 一、这项实验想解决什么问题

长对话模型会遇到两个互相矛盾的要求：

1. 很早以前出现的重要信息不能忘记；
2. 不能把对话里的每一句话都永久保存，否则存储会不断增长。

本项目提出的办法，是在固定大小的长期事实记忆前增加门控。模型读到一句话后，
自己判断它是否值得写入长期记忆；已经写入的内容也由保留门判断是否继续保留。
回答问题时，模型先从记忆槽位中寻找相关事实，再把事实融合进语言模型的隐藏状态。

可以把完整流程理解成：

```text
一句新消息
    ↓
是否包含事实？
    ↓
写入门：要不要长期保存？
    ↓
保留门：以后还要不要保留？
    ↓
语义检索：回答当前问题需要哪条事实？
    ↓
融合门：把检索结果多大程度送进解码器？
    ↓
语言模型生成答案
```

本实验重点检查前三个问题：

- 门控能否分辨“应当长期保存的事实”和“临时、应丢弃的事实”？
- 当临时事实多到超过槽位容量时，重要事实能否继续存活？
- 找到正确记忆后，模型能否生成完全正确的答案？

## 二、用什么方法进行比较

三个模型共享同一个 TinyLM 骨干，只改变长期记忆策略。

### 1. SWA+sink only

模型只保留：

- 最开始的 4 个 attention sink token；
- 最近的 124 个 token。

局部 KV 总预算是 128。更早且不属于 sink 的内容会被移出缓存，没有额外长期
事实记忆。该基线的缓存规则来自可审计的 StreamingLLM sink+recent 源码策略，
并适配到本项目 TinyLM。它不是论文作者官方发布的复现代码。

### 2. Fixed-LRU memory

模型拥有 8 个长期事实槽位，遇到有效事实候选就写入，不判断这条事实是否重要。
槽位满后，使用 LRU，也就是淘汰最久没有被访问的槽位。

这个模型回答的问题是：如果不做选择性写入，只使用固定容量加传统淘汰策略，会
发生什么？

### 3. Gated memory

模型同样拥有 8 个长期事实槽位，但由学习得到的写入门和保留门决定：

- 耐久事实应当写入并保留；
- 临时事实应当拒绝；
- 查询时只读取最相关的前 4 个槽位；
- 检索出的记忆通过学习式融合门进入解码器。

这就是当前希望验证的主要架构。

## 三、训练数据长什么样

每个结构化样本都遵循下面的顺序：

```text
耐久事实 → 若干条临时事实 → TinyStories 长文本 → 问题 → 答案
```

一个简化例子是：

```text
Store: Bob likes blue.            # 应长期保存
Discard: Mary likes gold.         # 临时事实，应丢弃
Discard: John likes red.           # 临时事实，应丢弃
[一段很长的 TinyStories 文本]
What does Bob like?
blue
```

耐久事实和临时事实都来自 user role，而且都使用相同的
`NAME likes VALUE` 形式。这样，模型不能只通过消息角色或“像不像一个事实”来
区分它们。

训练和验证使用不同提示词：

| 类型 | 训练模板 | 验证模板 |
|---|---|---|
| 应长期保存 | `Remember`、`Keep`、`Save` | `Store` |
| 应丢弃 | `Temporary`、`Skip`、`Ignore` | `Discard` |

所以验证不只是重复训练时的完整句子，也包含提示词改写。但是，这仍然是一项具有
明确重要性提示和监督标签的合成任务。它不能直接证明模型已经能在开放域对话中，
完全自主地理解任何信息的重要性。

### 容量与延迟压力

训练时：

- 临时事实数量：0、1、2、4；
- TinyStories 延迟段数：1、2、4。

验证时：

- 临时事实数量：0、2、4、8；
- TinyStories 延迟段数：1、4、16。

验证中的 8 条临时事实和 16 段延迟都超出了训练范围，用来检查容量与长度外推。
每个 `noise × delay` 条件有 24 个样本，共 12 个条件、288 个样本/模型。

## 四、模型与训练配置

| 项目 | 配置 |
|---|---|
| TinyLM 层数 | 6 |
| hidden size | 384 |
| attention heads | 8 |
| FFN size | 1,536 |
| 参数量 | 31,854,750，约 31.85M |
| tokenizer | GPT-2，词表 50,257 |
| 局部 KV 预算 | 128：4 sinks + 124 recent |
| 长期用户槽位 | 8 |
| 每次语义读取 | top-k=4 |
| 每轮最多候选 | 1 |
| payload 上限 | 12 token |
| 训练步数 | 2,000 |
| batch size | 2 |
| gradient accumulation | 2 |
| 每个 run 训练样本数 | 8,000 |
| 优化器 | AdamW |
| 主干学习率 | 3e-5 |
| 记忆模块学习率 | 3e-4 |
| 精度 | BF16 autocast |
| GPU | 单张 NVIDIA RTX 4090，物理 GPU 1 |
| checkpoint | 每 500 step 保存，另存 final |

三个条件都从同一个 seed-17 TinyStories 骨干开始。该骨干此前训练约 10M token，
TinyStories 验证 NLL 为 2.889852、PPL 为 17.9906。

## 五、如何理解这些指标

### Exact Match（EM）

模型最终生成的答案必须和标准答案完全一致，才算正确。例如标准答案是 `blue`，
生成 `red` 就是错误。EM 是最终任务正确率。

### 目标存活率

到回答问题之前，目标事实是否还存在于长期事实槽位中。它只检查“有没有忘记”，
不检查最后是否答对。

### Recall@1 和 Recall@4

- Recall@1：正确事实是否是语义检索排名第一的槽位；
- Recall@4：正确事实是否进入前四个读取槽位。

### MRR

MRR 反映正确槽位的平均排名。正确槽位总是第一名时，MRR=1。

### Active slots

实际处于活动状态的用户事实槽位数量。数量越小，说明门控越能避免保存无关内容。

## 六、总体实验结果

| 模型 | EM | 目标存活率 | Recall@1 | Recall@4 | MRR | 平均活动槽位 |
|---|---:|---:|---:|---:|---:|---:|
| Gated | 52.43% | **100.00%** | **100.00%** | **100.00%** | **1.0000** | **1.00** |
| Fixed-LRU | **74.65%** | 92.01% | 90.28% | 92.01% | 0.9103 | 4.25 |
| SWA-only | 5.21% | 不适用 | 不适用 | 不适用 | 不适用 | 0 |

这个结果包含两个看起来矛盾、但实际上同时成立的结论。

第一，Gated 的记忆选择是最好的。它在全部 288 条验证样本上都保住了正确事实，
而且每次都把正确槽位排在第一位。

第二，Gated 的最终答案并不是最好的。Fixed-LRU 的 EM 是 74.65%，明显高于
Gated 的 52.43%。因此当前实验不能宣称“门控使整体答案准确率最高”。

SWA-only 的 EM 只有 5.21%，表明只使用 128-token 局部缓存，基本无法解决
这个跨长文本的事实回忆任务。长期状态确实有必要。

## 七、容量压力下发生了什么

下面把三个不同延迟的结果按临时事实数量聚合。每行包含 72 个样本/模型。

| 临时事实数 | Gated EM | Gated 存活率 | Gated 槽位 | Fixed EM | Fixed 存活率 | Fixed 槽位 | SWA EM |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 0 | 48.61% | 100% | 1 | 73.61% | 100% | 1 | 2.78% |
| 2 | 51.39% | 100% | 1 | 83.33% | 100% | 3 | 6.94% |
| 4 | 54.17% | 100% | 1 | 80.56% | 100% | 5 | 4.17% |
| 8 | 55.56% | **100%** | **1** | 61.11% | **68.06%** | **8** | 6.94% |

Fixed-LRU 接受全部临时事实。一个目标事实加上 7 个临时事实刚好可以装满 8 个
槽位；加入第 8 个临时事实后必须淘汰某个槽位。因此在 noise=8 时，它的目标
存活率降到 68.06%，Recall@1 降到 61.11%。

Gated 拒绝全部临时事实，所以始终只有目标事实占用一个槽位。即使 noise=8，目标
存活率和 Recall@1 仍为 100%。这是当前实验对门控优势最直接的证据：

> 选择性写入阻止了临时信息占满固定容量，从而保护真正需要长期保存的事实。

## 八、长延迟下发生了什么

下面把四个临时事实条件按 delay 聚合。每行包含 96 个样本/模型。

| delay 段数 | Gated EM | Gated 存活/Recall@1 | Fixed EM | Fixed 存活/Recall@1 | SWA EM |
|---:|---:|---:|---:|---:|---:|
| 1 | 47.92% | 100% / 100% | 78.13% | 94.79% / 91.67% | 6.25% |
| 4 | 58.33% | 100% / 100% | 69.79% | 88.54% / 87.50% | 3.13% |
| 16 | 51.04% | **100% / 100%** | 76.04% | 92.71% / 91.67% | 6.25% |

Gated 只训练到 4 段延迟，但在 16 段时仍能完整保存和检索事实。这说明显式长期
状态不会因为中间 filler 变长而自然衰减。

这里的 EM 没有随 delay 单调下降。原因之一是不同 delay 网格使用不同的确定性
样本索引，并不是同一个事实只改变 filler 长度的严格配对实验。因此这些小幅波动
不应解释为“16 段一定比 4 段容易或困难”。

## 九、门控权重是否过高或过低

为回答这个问题，本报告另外在正式 Gated checkpoint 上重放了全部 288 条验证
样本，记录写入门、保留门和两个融合层的实际输出。

### 1. 写入门

| 输入类型 | 样本/候选数 | 最小值 | 平均值 | 最大值 | 硬决策结果 |
|---|---:|---:|---:|---:|---|
| 耐久事实 | 288 | 1.000000 | 1.000000 | 1.000000 | 全部写入 |
| 临时事实 | 1,008 | 3.23e-12 | 5.69e-7 | 2.62e-5 | 全部拒绝 |

写入门明显接近二值饱和：耐久事实接近 1，临时事实接近 0。但它在当前验证集上的
288 个目标和 1,008 个临时候选中没有出现错误硬决策。因此：

- 有“权重/概率很极端”的现象；
- 没有“极端值已经使当前任务退化”的证据；
- 极端分离很可能部分依赖明确的 `Store/Discard` 语言提示。

如果以后去掉直接提示、引入模糊重要性或混合用途事实，这种饱和可能造成置信度
校准差、决策难以调整，需要单独验证。

### 2. 保留门

目标事实的保留概率如下：

| 最小值 | 5%分位 | 平均值 | 95%分位 | 最大值 |
|---:|---:|---:|---:|---:|
| 0.999117 | 0.999189 | 0.999239 | 0.999281 | 0.999296 |

保留门也接近完全打开。对于本实验唯一的耐久事实，这是正确行为，因为目标存活率
为 100%。但是，本任务没有同时放入多条“曾经重要、后来过期”的事实，所以不能
判断保留门能否在需要时降低权重并释放旧槽位。

因此这里的准确说法是：当前没有观察到“保留权重太低导致遗忘”，但保留概率过于
接近 1，是否会导致未来难以遗忘陈旧事实仍未被测试。

### 3. 融合门

记忆会在 decoder 的两个层中进行融合。查询位置的融合门统计如下：

| 融合位置 | 全部样本均值 | 最小值 | 最大值 | 答对样本均值 | 答错样本均值 |
|---|---:|---:|---:|---:|---:|
| 第一个融合层 | 0.999295 | 0.997367 | 0.999797 | 0.999259 | 0.999335 |
| 第二个融合层 | 0.984094 | 0.920799 | 0.999731 | 0.989297 | 0.978358 |

融合门整体很高，尤其第一个融合层几乎始终完全打开。这意味着当前 EM 低并不是
因为融合门普遍接近 0、把正确记忆挡在解码器外。

答对与答错样本的第一个融合门均值只相差约 0.00008，而且答错样本反而略高。
第二个融合层中，答错样本平均值低约 0.01094，但两个分布仍然大量重叠，所有样本
也都保持较高融合。因此只能说“较弱融合可能与部分错误相关”，不能说它已经被
证明是错误原因。

### 4. 当前判断

综合三个门：

> 确实出现了接近 0/1 的门控饱和，但没有证据表明门控过高或过低是当前端到端
> 准确率退化的主要原因。

写入和保留门在当前任务上完成了预期功能；融合门也没有关闭正确记忆。要判断融合
权重是否“过高”，必须把同一个 checkpoint 的融合门强制为多个数值，例如
0、0.25、0.5、0.75、1.0，比较 EM 和答案 logits。仅凭自然观测到的门值不能作
因果判断。

## 十、为什么检索满分，最终答案仍只有 52.43%

为了定位瓶颈，本报告还检查了查询后第一个答案 token 在整个 50,257 词词表中的
排名。

| 诊断指标 | 结果 |
|---|---:|
| 正确答案 token 排名第一 | 52.43% |
| 正确答案 token 位于前五 | 97.57% |
| 正确答案平均排名 | 2.04 |
| 正确答案排名中位数 | 1 |
| 错误样本中，正确词距最高 logit 的平均差 | -0.5853 |

正确事实槽位每次都被排在记忆检索第一位，但正确答案词只在 52.43% 的样本里排在
语言模型词表第一位。这清楚地把两个排名区分开来：

```text
记忆槽位排名：正确事实始终第 1
词表输出排名：正确颜色平均约第 2，只有一半是第 1
```

错误输出不是主要由标点或大小写造成，而是模型经常生成另一个颜色词。137 个错误
样本中，99 个输出了 `yellow`，另外常见的错误输出是 `brown` 和 `silver`。
不同目标词的表现也很不均衡：`red`、`gold`、`yellow`、`amber` 达到 100%，而
`black`、`blue`、`green`、`white`、`brown` 在本次 Gated 验证中为 0%。

这表明当前主要瓶颈位于：

```text
已读出的正确 memory value/payload
        ↓
decoder 中的融合表示
        ↓
LM head 对具体答案词的 logit 排序
```

模型不是完全不知道正确答案。97.57% 的样本里，正确答案已经进入词表前五；但是
它与几个更强的颜色词先验之间还没有被可靠区分。换句话说，问题更接近“输出校准
和精确解码不足”，而不是“完全没有记忆”。

## 十一、为什么 Fixed-LRU 的 EM 反而更高

Fixed-LRU 的 EM 为 74.65%，但它的目标存活和读取指标低于 Gated。这说明“最终
生成能力”和“容量管理能力”是两个不同维度。

当前两个条件除写入策略外，还存在一个机制上的必要差异：

- Fixed-LRU 使用固定为 1 的融合门，检索结果始终全量注入；
- Gated 使用学习式融合门；
- Gated 还优化 write、retention、budget 等辅助损失；
- Fixed-LRU 不优化这些门控损失。

虽然 Gated 实际融合门也已经接近 1，但两种训练路径和梯度分配仍然不同。因此不应
把 22.22 个百分点的 EM 差距简单解释为“门控权重太低”，也不能解释为“LRU 比
门控更会保存信息”。在真正的超容量条件下，LRU 的目标存活率已经明显下降。

最重要的补充消融是：

1. `gated write + fixed fusion=1`：只保留选择性写入；
2. `fixed write + learned fusion`：只改变融合门；
3. 同一 Gated checkpoint 上扫描固定融合权重；
4. oracle 选择正确槽位并强制注入，检查答案上限。

这样才能把“写入门的价值”和“融合/生成训练不足”完全拆开。

## 十二、存储和计算效率

| 模型 | 训练时间 | 训练吞吐 | 平均逻辑槽位 bytes | peak allocated | peak reserved |
|---|---:|---:|---:|---:|---:|
| Gated | 22.05 min | 6.05 examples/s | 3,221 | 2.673 GB | 6.648 GB |
| Fixed-LRU | 20.01 min | 6.66 examples/s | 13,689.25 | 2.666 GB | 6.568 GB |
| SWA-only | 14.67 min | 9.09 examples/s | 0 | 2.871 GB | 6.881 GB |

Gated 平均只使用 1 个用户事实槽位，Fixed-LRU 平均使用 4.25 个。按活动槽位对应
的数据量计算，Gated 比 Fixed-LRU 少 76.47%。在 noise=8 时，两者分别为
3,221 和 25,768 bytes，Gated 少 87.50%。

但当前代码仍然为全部 8 个槽位预分配张量，所以 Gated 和 Fixed-LRU 报告的完整
`state_bytes` 都是 2,408,906 bytes。活动槽位减少只是逻辑稀疏，还没有自动变成
物理显存压缩。要声称真正压缩，需要进一步实现 packed/ragged slot storage，并
重新测量物理 state bytes、推理显存和延迟。

Gated 当前训练吞吐比 Fixed-LRU 低约 9.25%，比 SWA-only 低得更多。这是未融合
PyTorch 原型中门控、写入、保留和检索的实际开销，不能当作优化 kernel 后的最终
效率。

## 十三、实验现在支持哪些结论

### 已支持

1. 长期事实记忆相对 SWA-only 能显著提高长距离事实回答能力。
2. 门控能够在带明确语义提示的 held-out 模板上区分耐久事实与临时事实。
3. 选择性写入能避免临时事实耗尽固定槽位。
4. 在超容量压力下，Gated 的目标存活和检索明显优于 Fixed-LRU。
5. Gated 可以把训练范围外、16 段延迟前的目标保持并正确检索。
6. 记忆槽位选择问题已基本解决，下一瓶颈是记忆表示到输出词的转换。

### 尚未支持

1. Gated 的最终回答 EM 优于 Fixed-LRU。
2. 模型能在没有显式提示的开放域对话中自主判断重要性。
3. 模型能主动忘记曾经重要但已经失效的事实。
4. 逻辑稀疏已经带来物理显存或 KV 存储压缩。
5. 结果具有跨随机种子的统计稳定性。
6. 当前 checkpoint 是验证意义上的最佳 checkpoint。
7. 当前 TinyLM 数值可以直接与 1.3B--70B 模型上的论文数值比较。

## 十四、实验局限

- 当前只有 seed 17，没有三 seed 均值和标准差。
- 每个模型使用固定 2,000 step，保存 final checkpoint；没有按独立验证 EM 进行
  early stopping 或 best-checkpoint 选择。
- 重要性由人工构造的耐久/临时提示定义，任务仍比较简单。
- 每个样本只有一个真正应长期保存的事实，无法评价多条重要事实之间的取舍。
- 没有“事实后来失效”的样本，无法评价主动遗忘能力。
- delay 网格不是逐样本严格配对，不能从小幅非单调 EM 波动推断长度因果关系。
- 训练时间和峰值显存来自当前原型，不代表理论最优实现。

## 十五、下一步实验建议

建议按下面顺序继续，而不是直接扩大模型：

### 第一步：定位融合与输出瓶颈

- 扫描固定融合门 `0/0.25/0.5/0.75/1.0`；
- 增加正确槽位 oracle；
- 同时报告 teacher-forced NLL、自由生成 EM、正确词排名和 logit margin；
- 比较 `gated write + fixed fusion` 与完整 Gated。

这一步可以回答门控幅值是否真的导致生成退化。

### 第二步：改善精确答案解码

- 检查 payload 平均池化是否丢失具体值 token；
- 尝试对 payload token 做更直接的 cross-attention，而不是只依赖平均表示；
- 增加 answer-copy 或 value-token 对齐损失；
- 平衡不同颜色词在训练中的预测难度，检查输出词频偏置。

### 第三步：验证更真实的重要性判断

- 去掉 `Remember/Discard` 这类直接命令；
- 由后续是否被查询来定义重要性；
- 加入事实冲突、更新、失效和重新启用；
- 同时放入多条真正重要的事实。

### 第四步：完成统计和效率闭环

- 补 seed 29、43 的对应 TinyStories 骨干和三个策略；
- 报告三 seed 均值±标准差；
- 扫描 slots=4/8/16 和 noise/slots 比例；
- 实现物理紧凑槽位后，统一测量 prefill、decode latency、tokens/s 与峰值显存。

## 十六、一句话结论

本次 seed-17 pilot 已经证明：**门控模型能够拒绝临时事实，在固定容量被噪声挤压
时保护并正确检索长期事实。** 门控概率虽然接近 0 或 1，但在当前测试中没有产生
错误的写入、保留或检索决策；最终 EM 较低的更直接原因，是正确记忆进入解码器后，
正确答案词没有稳定成为词表最高 logit。下一阶段应优先解决融合表示与精确生成，
并用门值扫描和解耦消融检验门控饱和是否存在因果影响。

## 十七、结果与复现位置

正式结果均保存在数据盘，没有覆盖旧结果：

- Gated：`/root/autodl-tmp/26summerBDMI_transformer/runs/adaptive_gated_fact_memory_stress/selective_stress_v2_gated_s17_20260909/`
- Fixed-LRU：`/root/autodl-tmp/26summerBDMI_transformer/runs/adaptive_gated_fact_memory_stress/selective_stress_v2_fixed_lru_s17_20260909/`
- SWA-only：`/root/autodl-tmp/26summerBDMI_transformer/runs/adaptive_gated_fact_memory_stress/selective_stress_v2_swa_only_s17_20260909/`

每个正式目录都包含完整配置、2,000-step 日志、每 500-step checkpoint、final
checkpoint 和 288 条逐样本验证结果。当前代码的 15 项单元测试全部通过。

本报告是新增文件，没有修改此前的实验结果文件或报告。
